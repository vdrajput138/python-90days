Practice Questions (10)

Write a script to create a file called notes.txt and write 5 lines into it.

Write a script to read the file and print each line with line numbers.

Create a list of 10 names and write them to a file.

Write a script to append a new line to an existing file.

Write a script to check whether a file exists before reading it.

Write a helper function save_log(message) that appends logs into qa_log.txt with timestamps.

Write a function to read a CSV file and print the number of rows.

Handle exceptions when reading a file: show user-friendly messages.

Write a script that deletes a file only if the user confirms.

Create a function that writes test results (PASSED/FAILED) into a report file.
