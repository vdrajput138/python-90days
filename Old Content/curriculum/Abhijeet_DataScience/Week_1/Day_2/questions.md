📌 Day 2 — Pre-NumPy Math & Utility Skills

Implement custom abs() function.

Find all factors of a number.

Normalize a list of numbers to the scale 0-1 (formula).

Compute Euclidean distance between two points.

Convert temperature series (Celsius → Kelvin).

Given dict of marks, print subjects sorted by score.

Extract digits from string "abc123def" using loops.

Remove outliers using IQR logic (manual code).

Implement simple linear function y = mx + c.

Compute moving average using a loop.
