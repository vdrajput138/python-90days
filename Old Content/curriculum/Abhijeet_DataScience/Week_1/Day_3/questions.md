📌 Day 3 — Strings, Data Cleaning, Parsing, Thinking in Tidy Data
Practice Questions (10)

Clean a string by removing all special characters except letters and numbers.

Split "John|Doe|28" into fields and convert age to integer.

Given a messy list ["10", "20 ", " 30", "x", "40a"], convert valid numbers to int and ignore invalid ones.

Standardize a list of names by converting them to lowercase and removing surrounding spaces.

Extract email usernames from a list like ["a@x.com", "b@x.com"].

Count how many values in a list are numeric (int or float).

Replace missing values ("", " ", "NA", None, "null") with 0 in a list.

Merge two lists and return only the unique items.

Given "Price: $250 USD", extract the numeric value 250.

Given two lists of student IDs, print intersection and difference.
