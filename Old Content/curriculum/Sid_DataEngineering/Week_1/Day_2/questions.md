📌 Day 2 — Lists, Dicts, JSON, CSV

Given a CSV line: "1,John,50000" → parse to list manually.

Convert a dictionary to JSON using json.dumps().

Load a JSON string into a dict using json.loads().

Convert a list of dictionaries to JSON and write to employees.json.

Read a JSON file and print number of keys.

Write a script to merge two dictionaries.

Remove duplicates from a list using a set.

Convert tuple → list → add item → convert back to tuple.

Sort a list manually (write your own sorting logic).

Given a list of employee salaries, print only salaries > 50k.
