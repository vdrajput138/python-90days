📌 Day 5 — File I/O + Basic ETL Problems

Read a CSV file and print first 5 lines.

Write list of names to a file.

Append a log line with timestamp.

Write dictionary list into a JSON file.

Read JSON file and pretty-print it.

Delete a file only if user confirms.

Read a file and count words.

Handle file-not-found error gracefully.

Load a CSV file and print number of comma-separated fields.

Convert JSON → CSV (basic version).
