📌 Day 5 — File Handling + Pre-Pandas Data Operations
Practice Questions (10)

Read a text file and print only non-empty lines.

Write a list of dictionaries (students) into a JSON file.

Read a CSV file manually using .split(",") and count rows.

Extract only numeric columns from a CSV row.

Append ML experiment logs into a file with timestamps.

Read a file and remove all blank or whitespace-only lines.

Convert a JSON list to a CSV file (basic implementation).

Write a script that checks if a file exists before reading it.

Read a file and count how many lines contain the word "error".

Create a simple “dataset summary” file with fields:
total_rows, empty_lines, alphabetic_lines, numeric_lines.
