📌 Day 3 — Loops & Conditions (ETL-style problems)

Loop through a list of file extensions and print only .csv files.

Loop through a list of dicts and print employees with salary > 60k.

Print numbers divisible by both 3 and 5 between 1–100.

Create a function that checks whether a value exists in a dictionary.

Read a list of file sizes and print total size.

Print the first 10 Fibonacci numbers.

Implement logic to validate if a JSON contains specific keys.

Write loop to count how many records have status="active".

Implement a script that continues reading input until "STOP" is entered.

Use break to stop processing when salary > 100k is found.
