Practice Questions (10)

Write a script that reads a user’s name & prints it in uppercase.

Create variables of different types (int, float, str, bool) and print them.

Convert the string "45.67" to float and add 10 to it.

Write a script that checks if an entered number is even/odd.

Given a list of numbers, compute the sum without using sum().

Read three integers from input and print the maximum.

Create a list of filenames and print only .csv files.

Given "100" convert it to int and multiply by 3.

Write a script to reverse a string using a loop.

Create a dictionary representing a row of data (id, name, salary) and print the keys.
