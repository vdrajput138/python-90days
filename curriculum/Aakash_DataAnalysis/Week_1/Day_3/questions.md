📌 Day 3 — Data Cleaning Logic (pre-Pandas)

Replace all spaces in string with _.

Detect if value is missing ("", " ", "NA", "None").

Remove duplicates from a list of names.

Split "John|Doe|30" into parts.

Convert list of numbers to integers safely (handle errors).

Standardize names: "VirU" → "viru".

Extract numbers from "Price: $200".

Remove special characters from a string.

Compare two lists and print common items.

Count how many values in list are numeric.
