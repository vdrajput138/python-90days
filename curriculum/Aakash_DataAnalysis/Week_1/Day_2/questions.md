📌 Day 2 — Pre-Pandas Basics

Read a .txt file and show line numbers.

Write list of product names into a file.

Parse a comma-separated string into a list.

Convert dict → JSON and save.

Count words in a text file.

Check if a substring exists in a string.

Clean string: strip spaces, lowercase it.

Convert list of numbers to strings.

Extract year from "2025-01-15".

Write script to categorize numbers as positive/negative/zero.
